# 백엔드 공개 URL이 생기면 아래 값을 채워서 frontend/.env 로 복사한 뒤 `npm run dev` 재실행하세요.
# 미설정 시 http://localhost:9000 (로컬 개발용 기본값)이 사용됩니다.
#
# 예시:
#   VITE_API_TARGET=http://203.0.113.10:9000          # 포트포워딩된 공인 IP
#   VITE_API_TARGET=https://sleepcare-api.example.com  # 도메인 + HTTPS

VITE_API_TARGET=
